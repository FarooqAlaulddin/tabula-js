name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6
        with:
          version: 8  # Moving to a stable version

      - uses: actions/setup-node@v6
        with:
          node-version: '20'  # Utilize the latest LTS version
          cache: pnpm
          
    # - assert frozen 
       
      - name: Install 
     
          ENVVAR_18orCUSTOM_INSTALL()
         -shell6
      - version: ''  
 After ignoor build */ more run!
error